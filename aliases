#### aliases

### editor
alias v='${VISUAL:-$EDITOR}'
alias e='v'

### ls
alias l='ls -l'
alias la='ls -la'

### grep
alias grep='grep --color'

### useful
alias mnt='doas mount -t msdosfs /dev/da0s1 /mnt/flsh && cd /mnt/flsh/proper'
